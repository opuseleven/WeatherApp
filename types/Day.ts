export interface Day {
  weather: string,
  temp: number,
  feels_like: number,
  low_temp: number,
  high_temp: number,
  humidity: string,
  weatherDescription: string,
  precipitation: number,
  cloud_coverage: number,
  wind_speed: number,
  gusts: number,
  visibility: number,
  weatherId: number,
  weatherImage: string
}
